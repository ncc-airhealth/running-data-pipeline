# 6. 검토 및 배포

최종적으로 데이터를 배포합니다.

## 완료 조건

- [ ] 로컬 main 브랜치가 리모트와 동일한 최신 상태이다.
- [ ] Xet/LFS로 추적할 대용량·바이너리 데이터 파일들이 `.gitattributes`에 지정되어 있다.

## 방법

1. `git add .`
2. `git commit -m "<commit-message>"`
3. `git push`


# AI 추가 지침

- `git commit`, `git push`는 사용자(사람)의 승인 이후 실행한다.
